#include "header.h"

/**
 * main - Entry Point
 * @:
 * @:
 * Description:)?
 * return:
 */

/* question: should ve void, or will shell launch with options to take? */

int main(int ac, char **av[], char *env)
{
	/**
	 * create prompt
	 * keeps printing "$" unless output given then should only print output
	 * quits program until word "exit" is given"
	 * use getline for input? Check return value of function
	 */

	while()
	{

	}
}
